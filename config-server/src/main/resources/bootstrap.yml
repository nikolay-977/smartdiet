spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nikolay-977/smartdiet-config
          default-label: main
          search-paths: '{application}'
  application:
    name: config-server